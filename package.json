{
  "name": "ipfs-helpers-builder",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "tsc && node ./esbuild.mjs",
    "pub": "cd dist && npm publish --access public"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "ipfs-unixfs-importer": "12.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.21.3"
  }
}
